# MIKO-s-ERP 使用與維護說明

> **🤖 給 AI 助手 (For AI Assistants) 的閱讀與編輯指南**：
> 1. **架構限制**：本專案為無伺服器架構，使用 `sql.js` (In-memory SQLite) 並儲存於 LocalStorage。透過 `file://` 協議直開，**絕對不可**使用 ES Module (`import/export`)。
> 2. **全域變數**：所有宣告於 `js/` 資料夾內的模組化腳本，其函數與變數皆暴露於 Window 全域環境，請確保命名無衝突。
> 3. **開發約定**：處理資料庫需依賴 `js/02_db.js` 提供的 `q(sql, args)` 與 `exec(sql, args)`；操作 `exec()` 會觸發 `03_autosave.js` 自動存檔。
> 4. **版號更新約定**：當您對系統的邏輯或介面總結修改完畢後，**請務必修改 `index.html` 左上角 `<div class="logo-ver">` 裡的版號** (如：從 v2.0.0 升為 v2.0.1 等)，並於註解中紀錄。

---

本系統為「單機、單人、無後端伺服器」架構的輕量級 ERP 系統，專為設計與委外加工流程打造。所有資料庫運行於瀏覽器記憶體中，並透過本機功能（LocalStorage 與 File System Access API）進行存檔與備份。

---

## 🚀 1. 快速啟動 (如何開啟系統)

本系統**不需要**安裝任何 Web Server（如 Apache、Nginx 或 Node.js）。
1. 打開您的檔案總管，進入 `MIKO-s-ERP`（目前為 `d:\暫存區\`）資料夾。
2. 找到 `index.html` 檔案。
3. **直接點擊兩下**，或點擊右鍵選擇用 **Google Chrome** 或 **Microsoft Edge** 開啟（建議使用這兩款瀏覽器以完整支援本地檔案存取 API）。
4. 網址列會顯示 `file:///D:/暫存區/index.html`，系統會自動載入並進入「儀表板」畫面。

---

## 💾 2. 資料儲存與備份機制 (極度重要)

因為本系統沒有雲端伺服器，資料的安全與備份完全依賴您所使用的瀏覽器與本機硬碟。

### 自動存檔 (AutoSave) 與 NAS 同步
- **觸發時機**：當您在系統中新增、編輯或刪除任何資料時，系統會在 **0.8 秒後**自動執行存檔。
- **雙重保障機制**：
  1. 系統會先將資料快速序列化存入瀏覽器的 `LocalStorage`。
  2. **【已升級】** 若您已經設定了「存檔到 NAS」(詳見下方)，系統會**同步將資料寫入實體的 NAS .db 檔案中**。
- **存檔確認**：請隨時注意左側選單最下方的狀態指示器。當顯示 **「✓ 已儲存」** 以及上方顯示的 NAS 寫入時間時，代表自動存檔與同步寫入皆成功。
- **關閉網頁防護**：關閉分頁時，系統會強制並即時執行最後一次存檔。
- **💡 關於清除瀏覽器資料 (Clear Browsing Data)**：
  早期版本若清除瀏覽器快取將會遺失所有進度。但在 **目前新版架構中，只要您有設定「儲存到 NAS」，資料是以實體 `.db` 檔案存活在您的硬碟或 NAS 裡。**  
  就算您不小心清除了瀏覽器快取，只要重新打開網頁，點擊左下角「📂 讀取 (openFromNAS)」選回該 `.db` 檔案，所有進度都會一字不漏地回復！

### 手動匯出 / 備份到隨身碟或 NAS (推薦每日操作)
在左側邊欄最下方的「db-actions」區塊中：
1. **💾 儲存 (saveToNAS)**：
   - 點擊後會喚起檔案儲存對話框，您可以選擇將資料庫存入指定的資料夾（例如您的 NAS 網路磁碟機或外接硬碟），檔名預設為 `project-erp.db`。
   - **優勢**：在 Chrome/Edge 中，首次允許存取檔名後，後續點擊「儲存」會**直接寫入**該檔案，不再跳出視窗（靜默存檔）。
2. **📂 讀取 (openFromNAS)**：
   - 點擊後可選擇您先前匯出的 `.db` 備份檔。
   - 匯入後，系統將以大腦中的備份檔還原所有資料，同時該資料也會自動覆寫目前瀏覽器的 LocalStorage 存檔。

---

## 📁 3. 檔案架構說明 (開發者與維護者適用)

系統已從單一龐大的 HTML 重構為模組化架構，以利後續的閱讀與維護。未來若要修改功能，請至對應的目錄中修改。

```text
MIKO-s-ERP/
├── index.html          # 系統入口骨架，僅包含 HTML 結構與外部腳本載入
├── css/
│   └── style.css       # 系統的所有視覺樣式、色彩主題宣告
└── js/                 # 系統核心邏輯 (共 23 個模組)
    ├── 01_schema.js    # 資料庫 Schema (所有 CREATE TABLE 語法)
    ├── 02_db.js        # 資料庫核心驅動 (q, exec, sort 的底層實作)
    ├── 03_autosave.js  # 本地 LocalStorage 自動存檔機制
    ├── 04_utils.js     # 通用工具函數 (日期計算、數字格式化、狀態 Badge)
    ... (各業務模組，例如 09_quotes.js 處理報價單，11_orders.js 處理訂單)
    ├── 21_csv.js       # 所有 CSV 報表匯出的功能
    ├── 22_pdf.js       # PDF 報表列印排版功能
    └── 23_init.js      # 系統啟動進入點腳本 (載入 DB、佈景套用)
```

**⚠️ 腳本載入限制：**
由於系統運行在 `file://` 協定，**不支援** ES Module (`import`/`export`)。所有位於 `js/` 底下的模組，都是將函數暴露在「全域 (Global, window) 環境」下供交錯呼叫。新增函數時請確保函數名稱不互相衝突。

---

## 🛠️ 4. 日常功能修改與維護指南

### Q1: 如何新增一個欄位 (例如在「客戶資料」新增「統一編號」)？
1. **修改資料庫結構**：打開 `js/01_schema.js`，在 `CREATE TABLE IF NOT EXISTS customers` 的字串中加入 `tax_id TEXT`。
2. **處理舊資料庫升級**：由於前端可能已經有舊的 DB 存檔，打開 `js/23_init.js`，找到 `const migrations = [...]` 的陣列，加入一條 SQL 語句：`"ALTER TABLE customers ADD COLUMN tax_id TEXT"`。（程式會在啟動時利用 try-catch 自動防呆嘗試升級欄位）。
3. **修改儲存邏輯**：打開 `js/17_master.js` 找到 `saveCustomer` 的 SQL 執行 `exec()` 語句，加入相應的參數綁定。
4. **修改介面 HTML**：同時在 `showAddCustomer` 與 `showEditCustomer` 方法中加入 `tax_id` 的 `<input>` 輸入框。

### Q2: 如何改變系統的 UI 顏色？
打開 `css/style.css` 頂部的 `:root` 或是 `[data-theme="xxx"]` 區塊，修改對應的 CSS 變數（如 `--bg`, `--accent` 等顏色代碼）。不需要重新編譯，重新整理網頁 `F5` 即可立即生效。

### Q3: 如何加掛新的外部函式庫 (Library)？
在 `index.html` 檔案底部的 `<script>` 區段中，新增您的 CDN 連結（例如 Chart.js 或是新的 PDF 工具），並確保放置在 `23_init.js` 上方。

---

## 🚑 5. 常見問題排除 (Troubleshooting)

| 問題狀況 | 原因與解決方法 |
| --- | --- |
| **重新整理後，剛剛新增的資料不見了** | 可能是 `LocalStorage` 配額用盡或是瀏覽器阻擋了自訂存取。請按 `F12` 打開開發者工具的 Console，檢查是否有紅字錯誤。並養成點擊 `💾 儲存` 手動備份 `.db` 到硬碟的習慣。 |
| **點擊「讀取」備份檔案但畫面沒有反應** | 瀏覽器可能不支援 File System Access API (通常發生在舊版 Safari 或 Firefox)。請改用最新版本的 Google Chrome 瀏覽。 |
| **無法開啟 / 畫面一直停留在「初始化專案資料庫...」** | 檢查電腦是否「**沒有連上網路**」。系統啟動時需要從 CDN 抓取 `sql-wasm.js` 核心引擎與字體。若是完全離線的環境，請將 `sql-wasm.js` 與 `sql-wasm.wasm` 下載到本機目錄更改路徑。|
| **想要將資料帶到另一台電腦辦公** | 在 A 電腦點擊「💾 儲存」匯出 `project-erp.db` 到隨身碟。將隨身碟插入 B 電腦，打開 B 電腦的 `index.html`，點擊「📂 讀取」匯入該檔案即可無縫接軌。 |
